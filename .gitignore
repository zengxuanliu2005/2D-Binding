# Large MD outputs — keep these out of git (treat as read-only input)
*.xyz
*.dcd
*.cpt
*.psf
*.xml
dump.tsv

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Results scratch (commit only curated figures/tables)
results/scratch/

# OS
.DS_Store

# Large simulation outputs
outputs/

# Claude Code plan-mode local working files (same pattern as settings.local.json)
.claude/plans/
